
<% labeled_form_for(:comment, :url => comments_path) do |f| %>
  <%= error_messages_for :comment %>
  <%= f.text_area :body %>

  <% f.button_group do %>
    <%= submit_button "Create" %> or <%= link_to 'Cancel', comments_path %>
  <% end %>
<% end %>

